# 2018.0601 sprint planning
- Get heap allocation right: an object with `$heap->allocate($x) = addr`?
- Verify ELF code, entry point, allocations, etc
- Initial register setup for `%rdi`
- Test bytecode to print something simple
